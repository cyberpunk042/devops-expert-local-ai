# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment / secrets
.env
.env.local
*.key
*.pem

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# LocalAI models — runtime directory, entirely gitignored.
# Source of truth for model configs: config/models/*.yaml
# Binaries downloaded by: make setup / make model-qwen3 / make model-download
/models/
!config/models/
/backends/
# AICP runtime (agent PID, RAG database, DLQ, history)
.aicp/

# Build artifacts (sd.cpp compiled binaries)
builds/

# LocalAI persistent data — runtime only, regenerated by make kb-sync
data/assets/
data/collections/
data/outputs/
data/*.lock
data/*.pid
