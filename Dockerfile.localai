# Pinned version — update deliberately after testing: make setup-force
# To upgrade: change the version tag here, then run: make setup-force
FROM localai/localai:v4.0.0-gpu-nvidia-cuda-12

# Backend extracted from quay.io OCI image via scripts/extract-backend.sh.
# Run `make extract-backend` if backends/cuda12-llama-cpp/ does not exist.
COPY backends/cuda12-llama-cpp /backends/cuda12-llama-cpp
